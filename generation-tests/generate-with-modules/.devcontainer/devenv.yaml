# Test project configuration for generation testing
name: "Generation Test Project"

modules:
  - mise
  - scala  # Test language module

image: "mcr.microsoft.com/devcontainers/base:ubuntu"

forwardPorts:
  - 3000

plugins:
  vscode:
    - ms-python.python
  intellij:
    - com.intellij.plugins.markdown  # Additional plugin beyond module defaults

postCreateCommand:
  - logLine: "gen-test"
    cmd: "echo 'Container created'"
    workingDirectory: "/workspaces/test"
